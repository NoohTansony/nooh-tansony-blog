---
const today = new Date();
---

<footer>
	© {today.getFullYear()} nooh.tansony
</footer>

<style>
	footer {
		max-width: 900px;
		margin: 0 auto;
		padding: 3rem 1rem 4rem;
		color: rgb(var(--gray));
		font-size: 0.95rem;
		border-top: 1px solid rgb(var(--gray-light));
		background: linear-gradient(
			180deg,
			rgba(var(--bg), 0.2) 0%,
			rgba(var(--bg), 0.9) 100%
		);
	}
</style>
